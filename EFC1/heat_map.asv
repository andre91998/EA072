i=1;
H=zeros(10,10);
for 
    for j=1:1:784
        r=rem(j,28);
        if r==0
            i=i+1;
            r=1;
        end
        H(r,i)=W(j,10);

    end

heatmap(H,'GridVisible','off','Colormap',pink);